{
  "name": "khdema",
  "version": "0.1.0",
  "private": true,
  "description": "Artisan & Home Service Booking App — Tunisia",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "client": "npm run start --workspace=apps/client",
    "artisan": "npm run start --workspace=apps/artisan",
    "lint": "eslint apps/ packages/ --ext .ts,.tsx"
  }
}
